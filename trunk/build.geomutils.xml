<?xml version="1.0"?>
<project name="svn-geomutils" basedir="." default="help">
  
  <property file="build.properties"/>

  <property name="geomutils.output.dir" value="geomutils.classes"/>
  <property name="co.dir" value="src.geomutils"/>
  
  <!-- path to the svnant libraries. Usually they will be located in ANT_HOME/lib -->
  <path id="project.classpath">
	<fileset dir="${lib.dir}">
	  <include name="**/*.jar"/>
	</fileset>
  </path>  

  <path id="classpath">
	<fileset dir="all-3rd-party">
	  <include name="**/*.jar"/>
	</fileset>
  </path>  

  <target name="clean">
    <delete dir="${co.dir}"/>  
    <delete dir="${geomutils.output.dir}"/>  
  </target>

  <target name="all" depends="clean, co.geomutils.utilities, compile.geomutils, copy.geomutils, archive.geomutils"/>

  <target name="checkout.geomutils.utilities">
    <echo>Checking out GeomUtils</echo>
	<!--svn><checkout url="http://coreutilities.googlecode.com/svn/trunk coreutilities-read-only" path="src_geomutils" /></svn-->
	<svn><checkout url="http://geomutils.googlecode.com/svn/trunk" path="src_geomutils" /></svn>
  </target>
  
  <target name="co.geomutils.utilities">
    <exec executable="${svn.exec}" searchpath="yes">
      <arg value="checkout"/>
      <arg value="http://geomutils.googlecode.com/svn/trunk/"/>
      <arg value="${co.dir}"/>
    </exec>
  </target>
  
  <target name="compile.geomutils" description="Compile Java source files" depends="co.geomutils.utilities">
    <mkdir dir="${geomutils.output.dir}"/>
    <javac destdir="${geomutils.output.dir}" classpathref="classpath"
           debug="${javac.debug}" nowarn="${javac.nowarn}"
           deprecation="${javac.deprecation}" encoding="Cp1252" source="1.5"
           target="1.5">
      <compilerarg value="-Xlint:unchecked"/>
      <src path="${co.dir}/src"/>
    </javac>
  </target>
  
  <target name="copy.geomutils" description="Copy files to output directory" depends="compile.geomutils">
    <patternset id="copy.patterns">
      <include name="**/*.gif"/>
      <include name="**/*.jpg"/>
      <include name="**/*.jpeg"/>
      <include name="**/*.png"/>
      <include name="**/*.properties"/>
      <include name="**/*.xml"/>
      <include name="**/*-apf.xml"/>
      <include name="**/*.ejx"/>
      <include name="**/*.xcfg"/>
      <include name="**/*.cpx"/>
      <include name="**/*.dcx"/>
      <include name="**/*.sva"/>
      <include name="**/*.wsdl"/>
      <include name="**/*.ini"/>
      <include name="**/*.tld"/>
      <include name="**/*.tag"/>
      <include name="**/*.xlf"/>
      <include name="**/*.xsl"/>
      <include name="**/*.xsd"/>
      <include name="**/*.ttf"/>
    </patternset>
    <copy todir="${geomutils.output.dir}">
      <fileset dir="${co.dir}/src">
        <patternset refid="copy.patterns"/>
      </fileset>
    </copy>
  </target>
  
  <target name="archive.geomutils" depends="copy.geomutils">
    <!--exec executable="cmd" output="manifest.mf" append="false" os="Windows XP">
      <arg value="/c"/>
      <arg value="echo"/>
      <arg value="Compile-Date:"/>
      <arg value="%DATE%"/>
      <arg value="%TIME%"/>
    </exec-->
    <mkdir dir="all-libs"/>
    <jar destfile="all-libs/geomutils.jar" compress="true"> <!-- manifest="manifest.mf"-->
      <fileset dir="${geomutils.output.dir}">
        <exclude name="**/*.cdi"/>
      </fileset>
    </jar>
  </target>

</project>
