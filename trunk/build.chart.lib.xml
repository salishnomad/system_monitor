<?xml version="1.0"?>
<project name="svn-chart-lib" basedir="." default="help">
  
  <property file="build.properties"/>

  <property name="chart.lib.output.dir" value="chart.lib.classes"/>
  <property name="co.dir" value="src.chart.lib"/>
  
  <!-- path to the svnant libraries. Usually they will be located in ANT_HOME/lib -->
  <path id="project.classpath">
	<fileset dir="${lib.dir}">
	  <include name="**/*.jar"/>
	</fileset>
  </path>  

  <path id="classpath">
	<fileset dir="all-3rd-party">
	  <include name="**/*.jar"/>
	</fileset>
  </path>  

  <target name="clean">
    <delete dir="${co.dir}"/>  
    <delete dir="${chart.lib.output.dir}"/>  
  </target>
  
  <target name="all" depends="clean, co.chart.lib, compile.chart.lib, copy.chart.lib, archive.chart.lib"/>

  <target name="co.chart.lib">
    <exec executable="${svn.exec}" searchpath="yes">
      <arg value="checkout"/>
      <arg value="http://chart-library.googlecode.com/svn/trunk/"/>
      <arg value="${co.dir}"/>
    </exec>
  </target>
  
  <target name="compile.chart.lib" description="Compile Java source files" depends="co.chart.lib">
    <mkdir dir="${chart.lib.output.dir}"/>
    <path id="chart.lib.classpath">
      <path refid="classpath"/>
      <pathelement location="all-libs/coreutilities.jar"/>
      <pathelement location="all-libs/nmeaparser.jar"/>
      <pathelement location="all-libs/nmeareader.jar"/>
      <pathelement location="all-libs/chartcomponents.jar"/>
    </path>
    <javac destdir="${chart.lib.output.dir}" classpathref="chart.lib.classpath"
           debug="${javac.debug}" nowarn="${javac.nowarn}"
           deprecation="${javac.deprecation}" encoding="Cp1252" source="1.5"
           target="1.5">
      <compilerarg value="-Xlint:unchecked"/>
      <src path="${co.dir}/src"/>
    </javac>
  </target>
  
  <target name="copy.chart.lib" description="Copy files to output directory" depends="compile.chart.lib">
    <patternset id="copy.patterns">
      <include name="**/*.gif"/>
      <include name="**/*.jpg"/>
      <include name="**/*.jpeg"/>
      <include name="**/*.png"/>
      <include name="**/*.properties"/>
      <include name="**/*.xml"/>
      <include name="**/*-apf.xml"/>
      <include name="**/*.ejx"/>
      <include name="**/*.xcfg"/>
      <include name="**/*.cpx"/>
      <include name="**/*.dcx"/>
      <include name="**/*.sva"/>
      <include name="**/*.wsdl"/>
      <include name="**/*.ini"/>
      <include name="**/*.tld"/>
      <include name="**/*.tag"/>
      <include name="**/*.xlf"/>
      <include name="**/*.xsl"/>
      <include name="**/*.xsd"/>
    </patternset>
    <copy todir="${chart.lib.output.dir}">
      <fileset dir="${co.dir}/src">
        <patternset refid="copy.patterns"/>
      </fileset>
    </copy>
  </target>
  
  <target name="archive.chart.lib" depends="copy.chart.lib">
    <!--exec executable="cmd" output="manifest.mf" append="false" os="Windows XP">
      <arg value="/c"/>
      <arg value="echo"/>
      <arg value="Compile-Date:"/>
      <arg value="%DATE%"/>
      <arg value="%TIME%"/>
    </exec-->
    <mkdir dir="all-libs"/>
    <jar destfile="all-libs/chart.lib.jar" compress="true"> <!-- manifest="manifest.mf"-->
      <fileset dir="${chart.lib.output.dir}">
        <exclude name="**/*.cdi"/>
      </fileset>
    </jar>
  </target>

</project>
